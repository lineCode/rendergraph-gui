#include "screenspacepass.h"

namespace render {
	/*
ConstantParameter *ScreenSpacePass::addParameter(util::StringRef name,
                                                 float v) {

  return nullptr;
}

void ScreenSpacePass::removeParameter(util::StringRef name)
{
}

util::StringRef ScreenSpacePass::source() const {
  return util::StringRef{source_.c_str(), source_.size()};
}

void ScreenSpacePass::setSource(util::StringRef source) {
  source_.assign(source.ptr, source.len);
}*/
} // namespace render
